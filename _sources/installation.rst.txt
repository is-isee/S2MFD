Installation
============

現在は、S2MFDは開発中のため、PyPIには登録されていない。
そのため、GitHubから直接インストールする必要がある。

開発のためには、editableモードでインストールすることを推奨する。

.. code-block:: bash

    $ git clone https://github.com/is-isee/S2MFD.git
    $ cd S2MFD
    $ pip install -e .

これで、S2MFDがインストールされる。任意の場所からS2MFDをインポートすることができる。