# Rempel (2006) 実装の作業記録

2026-08-19 〜 2026-08-20。ブランチ `feature/rempel2006`。

関連文書:

- `2026-08-19_rempel2006_feasibility.md` — 着手前の実現可能性検討
- `2026-08-19_rempel_equations.md` — 原論文からの式の転記 (実装の仕様書)

---

## 1. ユーザーの要求仕様

1. 角運動量と質量 (音速抑制法を使うので ∫(ρ0 + ζ²ρ1) dV) を
   **machine precision で保存**すること
2. 全エネルギーは保存できないので、**変化をよく監視できる**ようにすること
3. **境界から保存量が出ていかない**ように注意すること (人工粘性も物理も)
4. 実装後も **kinematic dynamo の機能を残す**こと
5. 人工粘性で散逸した運動エネルギー・磁場エネルギーを
   **エントロピーの式に足す**のを忘れないこと

## 2. 達成状況

| 要求 | 状態 | 実測値 |
|---|---|---|
| 角運動量保存 (閉じた系) | **達成** | ΔL/L = 1.8e-17 |
| 角運動量収支 (Rempel の開いた境界) | **達成** | ΔL と境界フラックス積分が相対 9.8e-14 で一致 |
| 質量保存 | **達成** | ΔM/M = 1.9e-18 |
| エネルギー収支の監視 | **達成** | Rempel 式(20)-(34) を実装、論文表1と直接比較可能 |
| 境界フラックスのゼロ化 | **達成** | 全フラックスをリテラル 0.0 で上書き |
| kinematic 機能の維持 | **達成** | 参照実装とビット一致。147 テストが通過 |
| 散逸のエントロピーへの還元 | **達成** | 局所形 -F·∇u。係数は厳密に 1、正値性も離散レベルで保証 |
| 長時間積分の安定性 | **達成** | 論文値のパラメタで安定 (§5) |
| Rempel 2006 の定量再現 | **進行中** | 人工拡散の特性速度の誤りを修正して改善中 (§5b) |

---

## 3. 設計上の中心的な判断

### 3.1 保存量に Ω₀ を含めない (齋藤 2024 の定式化)

```
保存量   q_L  = ρ0 r⁴ sin³θ · Ω1          <- 摂動のみ
フラックス F  = ρ0 r⁴ sin³θ · (Ω0+Ω1) v   <- 全体を運ぶ
```

Ω₀ は定数なので両者は**解析的に等価**。しかし保存量に Ω₀ を含めると、
大きさ ∝Ω₀ の数に ∝Ω₁ の増分を毎ステップ加えることになり、Ω₁ の相対精度が
eps·(Ω₀/Ω₁) に落ちる。剛体回転から出発すると Ω₁/Ω₀ → 0 なので初期ほど悪い。

数値実験 (96×96、純移流、剛体回転から出発):

| 形式 | 総角運動量 (厳密に 0 のはず) | Ω₁ の相対誤差 |
|---|---|---|
| 素朴形 `ρ0r⁴sin³θ(Ω0+Ω1)` | 7.0e+24 | 1.0e-10 |
| 齋藤形 `ρ0r⁴sin³θ·Ω1` | 2.3e+16 (丸め1単位) | **2.0e-14** |

テスト `test_perturbation_form_beats_total_form` で固定。

### 3.2 保存形と移流形のずれ

保存形は移流形 ρ₀D(ϖ²Ω)/Dt に対して `-Ω₀ϖ²∇·(ρ₀v)` だけ余分な項を持つ。
質量流束が離散的に非発散なら消えるが、音速抑制法では
`∇·(ρ₀v) = -ζ²∂ρ₁/∂t` なので厳密にはゼロにならず、しかも係数 Ω₀ で
増幅される。圧縮性のある速度場で測ると、この項が保存形の右辺の **98%** を
占めた。

Rempel は角運動量方程式を**移流形**で書いており (2005 式 4)、これは
「厳密な保存」と「圧縮性誤差への Ω₀/Ω₁ 倍の感度」のトレードオフとして
まっとうな判断である。

対応として `consistent_advection=True` オプションを用意した。連続の式と
同一の質量流束の面値に比角運動量 ϖ²(Ω₀+Ω₁) の面値を掛ける形で、Ω が
一様なら角運動量方程式が質量方程式に帰着する (free-stream preservation)。
既定は齋藤形のまま。

### 3.3 厳密な保存が必要なのは質量と角運動量だけ

球座標では動径・子午面方向の運動量は幾何学的な源項 (遠心力・曲率) を
必然的に持つので保存量ではない。軸対称系で厳密に保存するのは回転軸まわりの
角運動量だけで、これは離散化でも厳密に保てる。齋藤コードも同じ構成
(`qqt2`, `qqt3` には源項があり、`qqt4` にはない)。

### 3.4 散逸のエントロピーへの還元

応力から Q = Σ(1/2)E_ik R_ik を別途離散化するのではなく、
**フラックスと勾配から局所的な変換率を直接作る**:

```
Q = -(F^r ∂_r u + F^θ ∂_θ u)/(r²sinθ)        ← 散逸項のフラックスのみ
∂s1/∂t += (γ-1)/p0 · Q
```

これは Rempel の Q の定義そのものである (境界フラックスがゼロなので
部分積分の表面項が消え、Q は「レイノルズ応力が流れにした仕事の符号反転」
に等しい)。粘性項は正の加熱、Λ 効果は負の加熱になり、原論文の記述
「Q は粘性散逸による加熱項と Λ 効果による冷却項を含み、後者が一般に支配的」
がそのまま再現される。

離散化は面フラックスと面での勾配を組にしてセル中心へ平均する。この形なら
拡散フラックス `F = -κJ Δu/Δr` に対して各項が `+κJ(Δu/Δr)² ≥ 0` に
なるので、**散逸の正値性が離散レベルで保証される**。

当初は「運動量方程式が奪った分」= `u·dq/dt` で評価していたが、これは
体積積分では正しくても局所的には発散の分だけずれ、角運動量では
Ω0/Ω1 倍の偽の加熱になる。§4.4(e) を参照。

### 3.5 人工拡散は保存形の枠組みに乗せる

齋藤コードにも人工粘性 `artdif.f90` があるが `mhd.f90` で呼び出しが
コメントアウトされている。理由は明白で、本体と**異なる保存量**
(`r³sin²θ·Ω1`、ρ₀ を含まない) に対して定義されており、幾何因子を
フラックスの外側に掛けているため、有効にすると角運動量保存が壊れる。

本実装は **拡散対象はプリミティブ変数、フラックスは保存量と同じ
ヤコビアンを掛けた形** にした:

```
F^r = -(1/2) c Φ · ρ0 r⁴ sin³θ · (Ω1[i] - Ω1[i-1])
```

物理粘性の `ν ∂_rΩ1` が `(1/2)cΦ ΔΩ1` に置き換わっただけなので、面で1回
計算して2セルで共有 → telescoping、境界面はリテラル 0.0、拡散されるのは
Ω1 そのもの、が同時に成り立つ。

保存量 q_L を直接拡散させないのは、極近傍では q_L の勾配が Ω1 ではなく
幾何因子 sin³θ の変化に支配される (第1セルと第2セルで 8 倍違う) ため。

---

## 4. 発見したこと

### 4.1 Rempel の下部境界条件は系を閉じない

Rempel 2005 §2.6:

> "At the bottom boundary (r = 0.65 R⊙) we enforce a uniform rotation;
> the top boundary is stress-free for the angular velocity (R_rφ = 0)."

つまり下部境界は **Ω₁ = 0 の Dirichlet 条件**であり、固定 Ω のリザーバとして
粘性フラックスがそこを通る。Rempel 自身が「物理的なタコクラインではなく
人為的な装置」と明言している:

> "we do not include a sophisticated theory for the tachocline but force a
> tachocline through a uniform rotation boundary at r = 0.65 R⊙. As a
> consequence the tachocline is a viscous shear layer between the convection
> zone and the lower boundary condition."

したがってユーザー要求「境界から保存量が出ていかないように」と Rempel の
設定は**そのままでは両立しない**。両方を提供することにした:

- `angmom_bottom_bc='stress_free'` — 閉じた系。∫q_L が machine precision で保存
- `angmom_bottom_bc='uniform_rotation'` — Rempel 再現。境界フラックスを
  RK2 と同じ重みで積算し、`ΔL = ∫(境界フラックス)dt` が machine precision で
  成立することを検証する (漏れを許すのではなく、漏れを厳密に勘定する)

実測: ΔL/L = 2.14e-4 の流入に対し、収支残差は相対 9.8e-14。

### 4.2 Rempel は人工粘性を使っていない

両論文を全文検索したが、artificial viscosity / hyperdiffusion / filter の
記述は一切ない。散逸はすべて物理的な ν_t, κ_t, η_t と、**MacCormack の
交互風上/風下差分に内在する数値散逸**による。

これは実装上決定的で、MacCormack が音波に対して持つ暗黙の散逸は

```
(1/2) c_eff Δx ≈ 0.5 × 2.56e5 × 3.64e8 ≈ 5e13 cm²/s
```

であり、物理的な乱流粘性 ν_t = 3e12 の **15 倍**にもなる。S2MFD の
SSP-RK2 + 中心差分は数値散逸をまったく持たないので、同等の散逸を
明示的に入れなければならない。

### 4.3 上部境界は 0.985 R⊙ では解けない

Rempel は r = 0.65 - 0.985 R⊙ を使うが、本実装では 0.985 R⊙ は不安定。
Rempel (2005) のポリトロープは 1.0007 R⊙ で密度ゼロに達するので、
0.985 R⊙ では ξ = 0.039 まで落ちて成層が破綻寸前になる。

実測 (0.30 年積分):

| 上部境界 | nx | dr/H_p (上端) | 結果 |
|---|---|---|---|
| 0.985 R⊙ | 64 | 0.66 | 発散 |
| 0.985 R⊙ | 128 | 0.35 | 発散 |
| **0.96 R⊙** | 64 | 0.28 | **安定** |
| 0.96 R⊙ | 128 | 0.14 | 安定 |
| 0.95 R⊙ | 64 | 0.23 | 安定 |

解像度を上げても直らないので、これは離散化の分解能ではなく**成層の
急峻さそのもの**の問題。齋藤 (2024) が 0.96 R⊙ を採用しているのも
同じ事情と考えられる。既定を 0.96 R⊙ にした。

### 4.4 潰したバグ

いずれも推測ではなく、物理項を一つずつ落とす切り分けで特定した。

**(a) エントロピーに人工拡散を掛け忘れていた**

s1 は中心差分で移流されるだけで散逸を持たないため格子ノイズが減衰せず、
状態方程式 `p1 = p0(γρ1/ρ0 + s1)` を通して圧力に乗り、ρ0 が小さい
対流層上部で `ρ0⁻¹∂_r p1` として巨大な加速に化ける。その速度がさらに s1 を
かき混ぜるので正のフィードバックになる。赤道近傍・上部対流層から壊れた。

**(b) レイノルズ応力の二重計上**

`angular_momentum_rhs` が応力を `dq_om` と `dq_stress` の両方に書いており、
呼び出し側でも `dq_om += ds_om` していたため、粘性と Λ 効果が 2 倍に
なっていた。**見た目には「それらしい」差動回転が出るので気付きにくい。**
境界フラックスの収支がちょうど 2 倍ずれることで発覚した。

**(c) 下部境界条件が実質無効だった**

`angmom_bottom_bc='uniform_rotation'` は Ω1=0 のゴーストを設定していたが、
境界フラックスを一律ゼロ化していたため何の効果もなく、`stress_free` と
ビット単位で同じ結果になっていた。

**(d) 拡散項の CFL に安全率を掛け忘れていた**

κ を 100 倍にした設定が静かに壊れていた。人工拡散自体も陽解法なので
CFL に算入するようにした。

**(e) 散逸加熱の局所的な形が間違っていた (これが不安定の真因)**

散逸加熱を「運動量方程式が奪った分」= `u · dq/dt` で評価していた。
体積積分では正しいが、**局所的には発散の分だけずれる**:

```
u dq/dt = -div(u F) + F·grad(u)
```

第1項は輸送で体積積分すると境界だけに残り、第2項が局所的な変換である。
角運動量では `u = Ω0 + Ω1` で `Ω0 >> Ω1` なので、このずれは正しい値の
**Ω0/Ω1 倍 (20〜100倍)** にもなり、しかも符号が空間的に振動する。
その結果エントロピー方程式に偽の双極子ができ、浮力を通じて計算を
不安定にしていた。

正しい局所形 `-F·grad(u)` に直したところ、**論文値の κ (増幅なし) のまま
安定になった**。この形なら Ω0 は微分で消えるので偽の加熱が生じない。
離散化も面フラックスと面での勾配を組にしたので、拡散フラックス
`F = -κJ Δu/Δr` に対して各項が `+κJ(Δu/Δr)² ≥ 0` となり、
**散逸の正値性が離散レベルで保証される**。

§5 に記した「κ×3 が必要」という結論はこの修正により不要になった。

### 4.5 CFL は音速・アルヴェン速度・流れの 3 つで決まる

音速抑制法が遅くするのは**音波だけ**で、アルヴェン速度は変わらない。
磁場が上部対流層 (ρ0 が小さい) に達すると、ζ をいくら上げても
アルヴェン速度が dt の下限を決める。実測 (128×128、B_φ=1.28e4 G):

| RSST | 実効音速 | v_A (最大) | dt | 律速 |
|---|---|---|---|---|
| ζ=100 | 2.6e5 | 8.4e3 | 132 s | 音速 |
| ζ=300 | 8.5e4 | 8.4e3 | 395 s | 音速 |
| ζ=1000 | 2.6e4 | 8.4e3 | 1315 s | 音速 |

磁場がタコクライン (ρ0 が大きい) にある限りアルヴェン速度は効かないが、
上部対流層 (ρ0=7.4e-3) に達すると v_A ≈ 3.3e4 になり ζ≈1000 で切り替わる。

参考: kinematic ダイナモの dt は `defaults.py` (J08) で 1.45e5 s = 1.68 日
(拡散律速)、`hotta10.py` で 2.29e3 s。力学モード (ζ=100) の dt=132 s は
J08 比で 1100 倍、hotta10 比で 17 倍のステップ数になる。

---

## 5. 安定性 — 解決済み

当初、Rempel の物理パラメタ (ν_t = κ_t = 3e12 cm²/s) では 64×64 で
0.4 年程度で発散した。切り分けの結果:

| 条件 | 結果 |
|---|---|
| baseline (Rempel パラメタ) | 発散 0.40 年 |
| SLD floor 0.3 / 0.5 / 1.0 (完全 Rusanov) | 発散 (0.50 / 0.90 / 0.40 年) |
| κ_t × 3 / × 10 | 安定 |
| ν_t × 10 | 安定 |
| ν_t × 3 かつ κ_t × 3 | 発散 1.0 年 |
| δ = -1e-4 (全域を亜断熱に) | 安定 |
| δ = 0 (完全断熱) | 発散 |
| Λ 効果 × 0.1 | 発散 (振幅依存ではない) |
| 緯度・動径解像度 2 倍 | 発散 (解像度の問題ではない) |
| 人工拡散なし | より早く発散 |

「人工拡散を強めても直らないのに κ_t を上げると直る」「解像度を上げても
直らない」という組合せが手がかりになった。**真因は §4.4(e) の散逸加熱の
局所形の誤り**で、これを直したら論文値のまま安定になった。

Rayleigh (Solberg-Hoiland) 安定性も確認したが、不安定セルが現れるのは
v_r が急増した**後**であり、原因ではなく結果だった。

`sld_floor` (部分的な風上化の下限) は結果的に不要になったが、
MacCormack 相当の散逸を明示的に入れる手段として実装は残してある
(既定 0.1)。

---

## 5b. 未解決の課題 — 差動回転の振幅

安定に長時間積分できるようになったが、**差動回転が論文の約 1/7 で頭打ちになる**。
解の**構造は正しい**が振幅だけが小さい。

### 到達している解 (108×144, 7.5 年、飽和済み)

| 量 | 本実装 | Rempel 2006 |
|---|---|---|
| (Ω_eq − Ω_pole)/Ω0 (表面) | **+0.039** | **0.27** |
| Ω の範囲 | 408 – 424 nHz | 300 – 470 nHz (図1b) |
| Q_Λ | 0.0025 L⊙ | 0.014 L⊙ |
| Q_ν^Ω/Q_Λ | 0.21 | 0.574 |
| Q_C/Q_Λ | 0.26 | 0.425 |
| Q_B/Q_Λ | 0.47 | 0.419 |
| 表面の子午面流 | 6.9 m/s | ~10 m/s |
| \|s1\| (無次元エントロピー) | 1e-7 | ~3e-5 と推定 |

構造は太陽的 (赤道が速い、極が遅い、緯度方向に単調、外側ほど速い) で、
Q_B/Q_Λ は論文とよく合う。振幅だけが揃わない。

### 潰した可能性

| 仮説 | 検証 | 結果 |
|---|---|---|
| Λ 効果の実装ミス | 子午面流を凍結して 1 次元の釣り合いだけにする | DR=+0.092 に達する。解析見積り ~0.1-0.16 と同桁 → **実装は正しい** |
| フラックス形の誤り | Rempel 式(9)-(11) の F_φ と保存形フラックスが代数的に一致するか手で確認 | **完全に一致** |
| 初期条件依存 (双安定) | DR=0.27 から出発 | 1 年で 0.028 に減衰 → **低い解が唯一の平衡** |
| 人工拡散が削っている | sld_floor を 0.1 → 0.03 → 0.01 | DR は 0.024 → 0.034 → 0.037 で**頭打ち** |
| 解像度不足 | 64×64 → 108×144 | DR は 0.024 → 0.039 で頭打ち |
| 「拡散項の粘性は 2%」の解釈違い | 全域で 2% にしてみる | DR=0.0375 (1.5 倍だけ)。しかも Q_ν^Ω/Q_Λ が 0.009 になり論文の 0.574 から大きく外れる → **現行の解釈 (放射層の下限) の方が論文と整合** |
| 亜断熱層の設定 | 論文図7 相当、層を厚く、より亜断熱に | いずれも DR=0.026-0.028 でほぼ変わらず |

### 原因 — 人工拡散の特性速度に音速を使っていた

定常状態でエントロピー方程式の収支を項別に測って判明した:

```
背景勾配 (生成源)  5.94e-14 /s
移流              2.04e-15
熱伝導            7.54e-15
合計              5.92e-14   <- 釣り合っていない
```

**物理的な項では生成源の 1.6% しか打ち消せておらず、残りを人工拡散が
消していた。**

原因は人工拡散 (SLD) の特性速度にすべての変数で音速 `|v| + c_s,eff` を
使っていたこと。s1 も Omega1 も音波では運ばれず流れに乗って移流されるだけ
なので、特性速度は `|v|` が適切である。音速を使うと拡散係数が

```
(1/2) * h * floor * c_eff * dx = 0.5 * 1 * 0.01 * 2.56e5 * 3.64e8 = 4.7e11
```

に達する。これが致命的だったのは、**Rempel のモデルが「拡散を意図的に
小さくした領域」に依存しているから**である:

- オーバーシュート層の乱流熱伝導 kappa_t は対流層値の 0.2% (~5e10)。
  エントロピー摂動をそこに溜めて Taylor-Proudman 制約を破るのが
  Rempel 2005 の中心的な機構
- 放射層の粘性 nu_dif は対流層値の 2% (6e10)。下部境界の Omega1=0 との間に
  タコクライン (粘性せん断層) を作るのが要

どちらも人工拡散 4.7e11 がその 7〜10 倍で、機構そのものを潰していた。

### 修正と効果

`cfg.entropy_sld_speed = 'flow'`, `cfg.angmom_sld_speed = 'flow'` (いずれも
既定) で流速ベースにする。拡散係数は ~4e9 となり、上記の小さな物理拡散より
十分小さくなる。

| | 修正前 | 修正後 |
|---|---|---|
| 人工拡散/Q_Λ | 1.00 | **0.001** |
| \|s1\| | 1.4e-7 で飽和 | 8.7e-7 (2年) で**増加継続** |
| Q_Λ (2年) | 0.0030 L⊙ | **0.0057 L⊙** |
| DR の振る舞い | 4年で 0.024 に飽和 | 飽和せず成長継続 |

修正後の成長 (64×64、エントロピーのみ修正した場合):

```
t=1yr DR=+0.0285   t=3yr DR=+0.0423
t=2yr DR=+0.0367   t=4yr DR=+0.0463
```

**収束先はまだ確認中** (60 年ランを実行中)。エントロピーが緯度方向に
溜まる時間スケールは数十年なので、Rempel の 0.27 に達するかは
長時間積分の結果を待つ必要がある。

### 教訓

人工拡散の特性速度は「その変数が何で運ばれるか」に合わせる必要がある。
音響系 (rho1, v_r, v_theta) には音速、移流される量 (Omega1, s1) には流速。
一律に音速を使うと、モデルが依存している「物理拡散を小さくした領域」を
黙って潰してしまう。しかも計算は安定なまま答えだけが変わるので気付きにくい。

## 6. 実装したモジュール

| ファイル | 内容 |
|---|---|
| `S2MFD/physics/conservative.py` | 面フラックス配列を明示的に持つ発散演算子。telescoping の 3 条件を強制 |
| `S2MFD/physics/hydro.py` | 連続の式 (RSST)、角運動量、子午面運動量、粘性応力、エントロピー、散逸加熱、CFL |
| `S2MFD/physics/artdif.py` | slope-limited diffusion (保存形)。部分的な風上化の下限つき |
| `S2MFD/physics/dynamic.py` | 時間積分ドライバ、境界条件 |
| `S2MFD/physics/energy.py` | Rempel 式(20)-(34) のエネルギー収支診断 |
| `S2MFD/stratification.py` | Rempel(2005) ポリトロープ、超断熱度 δ(r)、RSST、保存量ヤコビアン |
| `S2MFD/setup.py` (追記) | Λ 効果、乱流粘性・熱伝導、R06 磁気拡散、R06 BL α 効果 |
| `S2MFD/parameters/rempel06.py` | Rempel 2006 参照モデルのパラメタ |
| `tests/test_conservation.py` | 34 個の検証テスト |

### 6.1 全球格子での Λ 効果の対称性

Rempel も齋藤も北半球のみを解くので傾き角 λ の符号を考える必要がない。
S2MFD は全球 [0, π] を解くため、そのまま使うと赤道対称性が壊れる。

q_L が偶関数であるためには F^r が偶、F^θ が奇でなければならず、
幾何因子 sin²θ が偶であることから Λ_rφ は偶、Λ_θφ は奇である必要がある。
振幅 L は cosθ に比例して奇なので、傾き角を λ·sgn(cosθ) として南半球で
符号を反転させるとこの条件を満たす (物理的にも傾きは両半球で鏡像になる)。

なお Rempel 2005 は「λ > 0 のときは n > 2 が必要 (極でレイノルズ応力の
発散が正則になるため)」と述べており、2006 の参照モデルは n=3。
齋藤コードの n=2 は λ=15° と組み合わせるとこの条件を満たさない。

---

## 7. 引き継ぎメモ

- ブランチ `feature/rempel2006`。main はマージ済みの kinematic 版のまま
- 既存の kinematic ダイナモは無変更 (`dynamics='kinematic'` が既定)。
  `S2MFD.inference` (GA パラメタ推定) にも一切影響しない
- テストは `pytest tests/` で 144 個。`tests/test_conservation.py` が
  今回追加した 34 個
- 長時間ランのスクリプトは scratchpad の `relax2.py`
  (`python relax2.py <nx> <ny> <years> <bc> <tag>`)。
  出力は `results_rempel/<tag>/` に history.npz と state.npz
- **§5 の安定性が解決するまで、Rempel の数値との比較はできない**
